<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wsj.medical2025.mapper.DrugCompanyMapper">

    <select id="getCompanyAll" resultType="com.wsj.medical2025.pojo.DrugCompany" parameterType="com.wsj.medical2025.pojo.DrugCompany">
        SELECT
            company_id,
            company_name,
            company_phone,
            create_time,
            update_time
        FROM
            drugcompany
        <where>
            <if test="companyName != null and companyName != ''">
                AND company_name LIKE CONCAT('%', #{companyName}, '%')
            </if>
            <if test="companyPhone != null and companyPhone != ''">
                AND company_phone = #{companyPhone}
            </if>
        </where>
    </select>

    <insert id="addCompany" parameterType="com.wsj.medical2025.pojo.DrugCompany" useGeneratedKeys="true" keyProperty="companyId">
        INSERT INTO drugcompany (company_name, company_phone, create_time, update_time)
        VALUES (#{companyName}, #{companyPhone}, #{createTime}, #{updateTime})
    </insert>
</mapper>